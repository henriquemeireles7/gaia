# .moon/toolchain.yml — required tool versions for Moon's runner.
# Aligns with .prototools at the repo root.

$schema: 'https://moonrepo.dev/schemas/toolchain.json'

bun:
  version: '1.2.0'

typescript:
  syncProjectReferences: false
