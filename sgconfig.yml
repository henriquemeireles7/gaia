# sgconfig.yml — ast-grep project config (vision §Stack: AST-aware patterns)
#
# ast-grep is a Rust-based AST search/lint/rewrite tool using tree-sitter.
# Rules live as YAML files under .gaia/rules/ast-grep/ alongside the policy
# manifest (.gaia/rules/index.ts) and the script-tier checks
# (.gaia/rules/checks/) — one folder, one taxonomy.

ruleDirs:
  - .gaia/rules/ast-grep
